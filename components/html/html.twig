<div data-id="{{ data.id }}" data-type="{{ data.type }}" class="{{ wrapper_classes|join(' ') }}">
    {{ title_display }}
    <div class="{{ component_classes|join(' ') }}">
        {{ html }}
    </div>
</div>
